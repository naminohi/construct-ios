# Server Configuration
# WebSocket server URL - supports domain names, IPv4, and IPv6 addresses
#
# Examples:
#   Domain:  VITE_SERVER_URL=wss://construct-messenger.fly.dev
#   IPv4:    VITE_SERVER_URL=wss://66.241.124.8:443
#   IPv6:    VITE_SERVER_URL=wss://[2a09:8280:1::b9:e736:0]:443
#   Local:   VITE_SERVER_URL=ws://localhost:8080 (development only, not secure)
#
# Note: Use wss:// for secure connections (recommended), ws:// for local development only
VITE_SERVER_URL=wss://66.241.124.8:443

# Development settings
VITE_DEBUG=false

# WebSocket configuration (optional - uses defaults if not set)
# VITE_WS_RECONNECT_INTERVAL=3000
# VITE_WS_MAX_RECONNECT_ATTEMPTS=5
# VITE_WS_HEARTBEAT_INTERVAL=30000
